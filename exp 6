def copy_string(source, destination, index):
    if index < len(source):
        destination += source[index]
        return copy_string(source, destination, index + 1)
    return destination

source_string = input("Enter the source string: ")
destination_string = ""
result = copy_string(source_string, destination_string, 0)
print("Copied string:", result)
